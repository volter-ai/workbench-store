{
  "id": "tiddlywiki",
  "name": "TiddlyWiki",
  "icon": "BookOpen",
  "tint": "#5778d8",
  "install": "{ [ -f package.json ] || printf '{\"name\":\"tiddlywiki-app\",\"private\":true}\\n' > package.json; } && npm install --no-audit --no-fund tiddlywiki@5 && { [ -d wiki ] || ./node_modules/.bin/tiddlywiki wiki --init server; } && mkdir -p wiki/tiddlers && printf 'title: $:/config/tiddlyweb/host\\n\\n$protocol$//$host$/__app/tiddlywiki/' > 'wiki/tiddlers/$__config_tiddlyweb_host.tid'",
  "start": "./node_modules/.bin/tiddlywiki wiki --listen port=5307 host=127.0.0.1",
  "port": 5307,
  "singleOrigin": {
    "basePath": {
      "arg": "path-prefix=",
      "strip": false
    }
  },
  "window": {
    "w": 1100,
    "h": 720
  },
  "vendors": [],
  "notes": "a non-linear personal notebook; pure JavaScript on Node, so it also runs in a browser tab's container"
}
